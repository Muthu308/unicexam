// config.js
// If you deploy this HTML on the SAME Vercel project as the /api functions,
// leave API_BASE as "" (relative paths like /api/classes work as-is).
// If you host the frontend elsewhere, set this to your deployed API origin,
// e.g. "https://your-project.vercel.app"
window.API_BASE = "";
